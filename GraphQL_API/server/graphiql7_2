// have to add the values in parenthesis after addProject
mutation {
  addTask(title: "Reboot styling", weight: 2, description: "Add Bootstrap to reset the CSS styling") {
    id
    title
    weight
    description
  }
}
